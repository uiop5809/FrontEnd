import NOSSR from "@/components/common/NOSSR";
import MainPage from "@/components/main/MainPage";

const Main = async () => {
  return (
    <NOSSR>
      <MainPage />
    </NOSSR>
  );
};

export default Main;
