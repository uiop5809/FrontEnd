"use client";

import styled from "styled-components";

const AdCopyPage = async () => {
  return <Layout>adcopy</Layout>;
};

export default AdCopyPage;

const Layout = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  justify-content: center;
  align-items: center;
  margin-top: 5.5rem;
`;
