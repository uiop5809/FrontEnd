import NOSSR from "@/components/common/NOSSR";
import ItemPage from "@/components/trend/ItemPage";

const Item = async () => {
  return (
    <NOSSR>
      <ItemPage />
    </NOSSR>
  );
};

export default Item;
